name: SonarQube Analysis

on:
  push:
    branches:
      - master

jobs:
  sonar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Compile Java sources
        run: |
          mkdir -p Project/build/classes
          javac -cp Project/derbyclient-10.2.2.0.jar \
            -d Project/build/classes \
            Project/src/LMS/*.java

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          args: >
            -Dsonar.organization=ayesha-saeed-0
            -Dsonar.projectKey=ayesha-Saeed-0_Library-Management-System-JAVA
            -Dsonar.sources=Project/src
            -Dsonar.java.binaries=Project/build/classes
